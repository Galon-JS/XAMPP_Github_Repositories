import type { Express } from 'express';
/** A nice wrapper for express.listen() */
export declare class ExpressListen {
    private app;
    private port;
    private httpServer;
    private sockets;
    constructor(app: Express);
    /** Immediately kill the server. */
    kill(): Promise<void>;
    listen(port: number, open?: boolean): Promise<number>;
    /** Tries to listen on a free port. */
    private tryToListen;
}
